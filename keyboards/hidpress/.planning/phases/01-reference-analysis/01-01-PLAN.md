---
phase: 01-reference-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-reference-analysis/EXTRACTED-DATA.md
autonomous: true

must_haves:
  truths:
    - "Sofle rev1 Vial config.h MATRIX_ROWS value is extracted alongside its vial.json rows value"
    - "Bipedal Ambi config.h MATRIX_ROWS and both vial.json rows values are extracted"
    - "Bipedal Southpaw rows match is confirmed as sanity check"
  artifacts:
    - path: ".planning/phases/01-reference-analysis/EXTRACTED-DATA.md"
      provides: "Raw extracted values from all source files, organized by keyboard"
      contains: "MATRIX_ROWS"
  key_links:
    - from: "EXTRACTED-DATA.md"
      to: "01-RESEARCH.md"
      via: "Extracted values confirm or contradict research claims"
      pattern: "rows.*10"
---

<objective>
Read all Sofle rev1 Vial source files and all Bipedal Ambi source files, extract raw configuration values, and write them to a structured extraction document. This plan does NOT create the final reference analysis -- it only extracts and records raw data for Plan 02 to synthesize.

Purpose: Isolate the file-reading work (13 source files) from the document-writing work, keeping context usage within budget. The research phase identified key findings (Sofle rows=10 matching MATRIX_ROWS=10; Ambi has rows=4 vs MATRIX_ROWS=8 mismatch). This plan verifies those claims directly from source files.

Output: `.planning/phases/01-reference-analysis/EXTRACTED-DATA.md` containing raw values from every source file, plus research verification notes.
</objective>

<execution_context>
@/Users/matthewthomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewthomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reference-analysis/01-RESEARCH.md
@.planning/phases/01-reference-analysis/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Sofle rev1 Vial source files and extract values</name>
  <files>
    keyboards/sofle/rev1/keyboard.json
    keyboards/sofle/rev1/keymaps/vial/vial.json
    keyboards/sofle/rev1/keymaps/vial/config.h
    keyboards/sofle/rev1/keymaps/vial/keymap.c
    keyboards/sofle/rev1/keymaps/vial/rules.mk
  </files>
  <action>
Read each Sofle rev1 Vial file listed above and extract the following data points. Record all values in working notes for Task 3.

From `keyboard.json`:
- `matrix_pins.rows` array (expect 5 pins)
- `matrix_pins.cols` array (expect 6 pins)
- Whether `split.enabled` is true

From `keymaps/vial/vial.json`:
- `matrix.rows` value (expect 10)
- `matrix.cols` value (expect 6)
- All key position strings in the `layouts.keymap` array -- extract every "row,col" reference and categorize:
  - Left-half keys (rows 0-4)
  - Right-half keys (rows 5-9)
  - Encoder entries (identified by `\n\n\n\n\n\n\n\n\ne` suffix)
- Whether an `"encoders"` JSON key exists at the top level (expect: no)

From `keymaps/vial/config.h`:
- VIAL_UNLOCK_COMBO_ROWS values (expect rows spanning both halves)
- VIAL_UNLOCK_COMBO_COLS values

From `keymaps/vial/keymap.c`:
- `encoder_map` array dimensions: how many encoders per layer, how many layers
- `keymaps` array dimensions: `[layers][MATRIX_ROWS][MATRIX_COLS]`

From `keymaps/vial/rules.mk`:
- ENCODER_MAP_ENABLE value
- VIA_ENABLE and VIAL_ENABLE values
  </action>
  <verify>
All 5 Sofle files have been read. Every data point listed above has a concrete value extracted.
  </verify>
  <done>Sofle rev1 Vial configuration values are fully extracted with concrete numbers for every data point.</done>
</task>

<task type="auto">
  <name>Task 2: Read Ambi + Southpaw source files, verify research, write EXTRACTED-DATA.md</name>
  <files>
    keyboards/hidpress/bipedalambi/config.h
    keyboards/hidpress/bipedalambi/info.json
    keyboards/hidpress/bipedalambi/keymaps/vial_left/vial.json
    keyboards/hidpress/bipedalambi/keymaps/vial_right/vial.json
    keyboards/hidpress/bipedalambi/keymaps/vial_left/config.h
    keyboards/hidpress/bipedalambi/keymaps/vial_right/config.h
    keyboards/hidpress/bipedalsouthpaw/config.h
    keyboards/hidpress/bipedalsouthpaw/keymaps/vial/vial.json
    .planning/phases/01-reference-analysis/EXTRACTED-DATA.md
  </files>
  <action>
**Step 1: Read Ambi files.** Extract:

From `bipedalambi/config.h`:
- MATRIX_ROWS (expect 8), MATRIX_COLS (expect 8)

From `bipedalambi/info.json`:
- matrix_pins.rows (expect 4 pins per side), matrix_pins.cols (expect 8 pins)
- split.enabled (expect true)

From `keymaps/vial_left/vial.json`:
- matrix.rows (expect 4, which is WRONG), matrix.cols (expect 8)
- All key position "row,col" references -- categorize left-half keys (rows 0-3) and thumbstick positions (col 7)

From `keymaps/vial_right/vial.json`:
- matrix.rows (expect 4, which is WRONG), matrix.cols (expect 8)
- All key position "row,col" references -- note that right-half keys currently use rows 0-3 (WRONG, should be 4-7)
- Encoder entries (with `\ne` suffix) -- extract their position references
- Whether an "encoders" JSON key exists (expect: yes)

From `keymaps/vial_left/config.h` and `keymaps/vial_right/config.h`:
- VIAL_UNLOCK_COMBO_ROWS and COLS values

From `bipedalsouthpaw/config.h` and `bipedalsouthpaw/keymaps/vial/vial.json`:
- MATRIX_ROWS and vial.json rows -- confirm they match (known-good non-split reference)

**Step 2: Verify research claims.** Compare every extracted value against what 01-RESEARCH.md states. Flag any discrepancies. The research claims:
- Sofle has 5 row pins, 6 col pins per side
- vial.json rows=10, cols=6
- Right-half layout positions use rows 5-9
- VIAL_UNLOCK_COMBO_ROWS { 0, 5, 3, 8 }
- encoder_map has 2 entries per layer across 4 layers
- No "encoders" JSON key in Sofle vial.json

**Step 3: Write EXTRACTED-DATA.md.** Create `.planning/phases/01-reference-analysis/EXTRACTED-DATA.md` with three sections:

1. **Sofle rev1 Vial -- Raw Values** -- all values from Task 1, organized as a flat table (property | value | source file)
2. **Bipedal Ambi -- Raw Values** -- all values from Step 1, organized as a parallel table (property | value | source file | expected | match?)
3. **Bipedal Southpaw Sanity Check** -- rows match confirmation
4. **Research Verification** -- list each research claim and whether it was confirmed, with any discrepancies flagged

Use compact tables, not narrative. Every value must cite its source file path.
  </action>
  <verify>
File `.planning/phases/01-reference-analysis/EXTRACTED-DATA.md` exists and contains:
- Sofle data table with matrix.rows=10 confirmed
- Ambi data table with matrix.rows=4 flagged
- Southpaw sanity check
- Research verification section with pass/fail for each claim
  </verify>
  <done>All source file values are extracted into EXTRACTED-DATA.md with research claims verified. Ready for Plan 02 to synthesize into REFERENCE-ANALYSIS.md.</done>
</task>

</tasks>

<verification>
Phase 1 partial check (data extraction only):
1. Every Sofle file has been read and values recorded
2. Every Ambi file has been read and values recorded
3. Research claims have been verified against actual values
4. EXTRACTED-DATA.md exists with all raw data in tabular format
</verification>

<success_criteria>
- EXTRACTED-DATA.md exists with verified raw data from all 13 source files
- Every value cited has a source file path
- Research verification section confirms or flags each claim from 01-RESEARCH.md
- All right-half layout positions from right vial.json are listed with their current row numbers
</success_criteria>

<output>
After completion, create `.planning/phases/01-reference-analysis/01-01-SUMMARY.md`
</output>
