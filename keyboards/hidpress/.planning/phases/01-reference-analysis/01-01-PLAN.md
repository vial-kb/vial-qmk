---
phase: 01-reference-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md
autonomous: true

must_haves:
  truths:
    - "Sofle rev1 Vial config.h MATRIX_ROWS value is documented alongside its vial.json rows value, confirming they match"
    - "Sofle rev1 vial.json layout positions for the right half are documented, showing which row numbers the right side uses"
    - "Sofle rev1 encoder mapping in vial.json is documented as reference for the Bipedal Ambi right-half encoder"
  artifacts:
    - path: ".planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md"
      provides: "Structured reference document with Sofle matrix dimensions, layout positions, and encoder mapping"
      contains: "rows.*10"
  key_links:
    - from: "REFERENCE-ANALYSIS.md"
      to: "01-RESEARCH.md"
      via: "Verified source data replaces research claims"
      pattern: "Sofle.*MATRIX_ROWS.*10"
---

<objective>
Read all Sofle rev1 Vial source files and all Bipedal Ambi source files, verify the research findings against actual file contents, and produce a structured reference analysis document with raw data tables.

Purpose: The research phase identified the key findings (Sofle uses rows=10 matching MATRIX_ROWS=10; Ambi has rows=4 vs MATRIX_ROWS=8 mismatch). This plan verifies those claims directly from source files and creates the formal reference document that Phase 2+ will cite. The document must contain raw values -- not just conclusions -- so downstream phases never need to re-read the Sofle files.

Output: `.planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md` containing verified matrix dimensions, layout position maps, and encoder configuration for both keyboards.
</objective>

<execution_context>
@/Users/matthewthomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewthomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reference-analysis/01-RESEARCH.md
@.planning/phases/01-reference-analysis/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read and verify Sofle rev1 Vial source files</name>
  <files>
    keyboards/sofle/rev1/keyboard.json
    keyboards/sofle/rev1/keymaps/vial/vial.json
    keyboards/sofle/rev1/keymaps/vial/config.h
    keyboards/sofle/rev1/keymaps/vial/keymap.c
    keyboards/sofle/rev1/keymaps/vial/rules.mk
  </files>
  <action>
Read each Sofle rev1 Vial file listed above and extract the following data points, recording them for use in Task 2:

From `keyboard.json`:
- `matrix_pins.rows` array (expect 5 pins)
- `matrix_pins.cols` array (expect 6 pins)
- Whether `split.enabled` is true

From `keymaps/vial/vial.json`:
- `matrix.rows` value (expect 10)
- `matrix.cols` value (expect 6)
- All key position strings in the `layouts.keymap` array -- extract every "row,col" reference and categorize them:
  - Left-half keys (rows 0-4)
  - Right-half keys (rows 5-9)
  - Encoder entries (identified by `\n\n\n\n\n\n\n\n\ne` suffix)
- Whether an `"encoders"` JSON key exists at the top level (expect: no)

From `keymaps/vial/config.h`:
- VIAL_UNLOCK_COMBO_ROWS values (expect rows spanning both halves)
- VIAL_UNLOCK_COMBO_COLS values

From `keymaps/vial/keymap.c`:
- `encoder_map` array dimensions: how many encoders per layer, how many layers
- `keymaps` array dimensions: `[layers][MATRIX_ROWS][MATRIX_COLS]`

From `keymaps/vial/rules.mk`:
- ENCODER_MAP_ENABLE value
- VIA_ENABLE and VIAL_ENABLE values

**Verification against research claims:** After extracting all values, compare each to what 01-RESEARCH.md states. Flag any discrepancies. The research claims:
- Sofle has 5 row pins, 6 col pins per side
- vial.json rows=10, cols=6
- Right-half layout positions use rows 5-9
- VIAL_UNLOCK_COMBO_ROWS { 0, 5, 3, 8 }
- encoder_map has 2 entries per layer across 4 layers
- No "encoders" JSON key in vial.json
  </action>
  <verify>
All 5 Sofle files have been read. Every data point listed above has a concrete value extracted. Any discrepancy between extracted values and 01-RESEARCH.md claims is flagged.
  </verify>
  <done>Sofle rev1 Vial configuration is fully extracted with verified raw values ready for documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Read Ambi source files, compare, and write REFERENCE-ANALYSIS.md</name>
  <files>
    keyboards/hidpress/bipedalambi/config.h
    keyboards/hidpress/bipedalambi/info.json
    keyboards/hidpress/bipedalambi/keymaps/vial_left/vial.json
    keyboards/hidpress/bipedalambi/keymaps/vial_right/vial.json
    keyboards/hidpress/bipedalambi/keymaps/vial_left/config.h
    keyboards/hidpress/bipedalambi/keymaps/vial_right/config.h
    keyboards/hidpress/bipedalsouthpaw/config.h
    keyboards/hidpress/bipedalsouthpaw/keymaps/vial/vial.json
    .planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md
  </files>
  <action>
**Step 1: Read Ambi files.** Extract the same data points from the Bipedal Ambi files:

From `bipedalambi/config.h`:
- MATRIX_ROWS (expect 8), MATRIX_COLS (expect 8)

From `bipedalambi/info.json`:
- matrix_pins.rows (expect 4 pins per side), matrix_pins.cols (expect 8 pins)
- split.enabled (expect true)

From `keymaps/vial_left/vial.json`:
- matrix.rows (expect 4, which is WRONG), matrix.cols (expect 8)
- All key position "row,col" references -- categorize left-half keys (rows 0-3) and thumbstick positions (col 7)

From `keymaps/vial_right/vial.json`:
- matrix.rows (expect 4, which is WRONG), matrix.cols (expect 8)
- All key position "row,col" references -- note that right-half keys currently use rows 0-3 (WRONG, should be 4-7)
- Encoder entries (with `\ne` suffix) -- extract their position references
- Whether an "encoders" JSON key exists (expect: yes)

From `keymaps/vial_left/config.h` and `keymaps/vial_right/config.h`:
- VIAL_UNLOCK_COMBO_ROWS and COLS values

From `bipedalsouthpaw/config.h` and `bipedalsouthpaw/keymaps/vial/vial.json`:
- MATRIX_ROWS and vial.json rows -- confirm they match (known-good non-split reference)

**Step 2: Create REFERENCE-ANALYSIS.md.** Write `.planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md` with these sections:

1. **Header** -- title, date, purpose statement
2. **Sofle rev1 Vial Reference Data** -- all raw values from Task 1 in a table
3. **Bipedal Ambi Current State** -- all raw values from Step 1 in a parallel table
4. **Bipedal Southpaw Validation** -- rows match confirmation (non-split sanity check)
5. **Side-by-Side Comparison Table** -- Sofle vs Ambi LEFT vs Ambi RIGHT with columns for: property, Sofle value, Ambi value, match?, action needed
6. **Matrix Row Doubling Convention** -- summarize the pattern with the QMK source code evidence (matrix.c ROWS_PER_HAND formula)
7. **EEPROM Layer Size Impact** -- show the formula `layer_size = rows * cols * 2` and calculate current (64 bytes) vs correct (128 bytes) for Ambi
8. **Encoder Configuration Comparison** -- Sofle approach vs Ambi approach, what must stay the same
9. **Right-Half Layout Position Map** -- list every position string from the right vial.json, showing current row and what it must change to
10. **Thumbstick Position Verification** -- confirm rows 0-3 col 7 remain valid after fix

Use tables, not narrative paragraphs, for all data. Include source file paths for every value so downstream phases can verify.

The document should be self-contained: a reader should be able to understand the full matrix convention and what needs to change without reading any other file.
  </action>
  <verify>
File `.planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md` exists and contains:
- Sofle data table with matrix.rows=10 confirmed
- Ambi data table with matrix.rows=4 flagged as wrong
- Side-by-side comparison table
- Right-half layout position map with current and target row numbers
- Encoder configuration comparison
- EEPROM layer size calculation (64 vs 128 bytes)
  </verify>
  <done>REFERENCE-ANALYSIS.md is complete with all verified raw data, comparison tables, and right-half position map ready for Phase 2+ to consume.</done>
</task>

</tasks>

<verification>
Phase 1 Success Criteria check:
1. Sofle rev1 Vial config.h MATRIX_ROWS value documented alongside vial.json rows value, confirming they match -- verify REFERENCE-ANALYSIS.md contains "rows: 10" for both sources
2. Sofle rev1 vial.json layout positions for right half documented with row numbers -- verify the document lists rows 5-9 for right-half keys
3. Sofle rev1 encoder mapping documented -- verify encoder configuration comparison section exists
4. "What Sofle does that Ambi must also do" checklist exists -- verified in Plan 02
</verification>

<success_criteria>
- REFERENCE-ANALYSIS.md exists with verified raw data from all source files
- Every value cited in the document has a source file path
- No unresolved discrepancies between research claims and actual file contents
- Right-half layout position map lists all ~25 key positions with current and target row numbers
</success_criteria>

<output>
After completion, create `.planning/phases/01-reference-analysis/01-01-SUMMARY.md`
</output>
