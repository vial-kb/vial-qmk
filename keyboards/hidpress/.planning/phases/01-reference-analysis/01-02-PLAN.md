---
phase: 01-reference-analysis
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md
autonomous: true

must_haves:
  truths:
    - "Sofle rev1 Vial config.h MATRIX_ROWS value is documented alongside its vial.json rows value, confirming they match"
    - "Sofle rev1 vial.json layout positions for the right half are documented, showing which row numbers the right side uses"
    - "Sofle rev1 encoder mapping in vial.json is documented as reference for the Bipedal Ambi right-half encoder"
  artifacts:
    - path: ".planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md"
      provides: "Structured reference document with side-by-side comparison, position maps, and EEPROM calculations"
      contains: "rows.*10"
  key_links:
    - from: "REFERENCE-ANALYSIS.md"
      to: "EXTRACTED-DATA.md"
      via: "Synthesizes raw extracted values into comparison tables and analysis"
      pattern: "MATRIX_ROWS"
---

<objective>
Synthesize the raw extracted data (from Plan 01) into a structured reference analysis document with comparison tables, position maps, and EEPROM calculations. This is the formal reference that Phase 2+ will cite.

Purpose: Transform raw data into actionable analysis. The document must contain side-by-side comparisons, the right-half position map (current vs target rows), and EEPROM size calculations so downstream phases never need to re-read source files or re-derive conventions.

Output: `.planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md` containing verified comparisons, position maps, and encoder configuration analysis.
</objective>

<execution_context>
@/Users/matthewthomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewthomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-reference-analysis/01-CONTEXT.md
@.planning/phases/01-reference-analysis/01-01-SUMMARY.md
@.planning/phases/01-reference-analysis/EXTRACTED-DATA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create side-by-side comparison and convention analysis sections</name>
  <files>
    .planning/phases/01-reference-analysis/EXTRACTED-DATA.md
    .planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md
  </files>
  <action>
Read EXTRACTED-DATA.md (from Plan 01) and create `.planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md` with the following sections:

1. **Header** -- title, date, purpose statement
2. **Sofle rev1 Vial Reference Data** -- pull all Sofle raw values from EXTRACTED-DATA.md into a summary table
3. **Bipedal Ambi Current State** -- pull all Ambi raw values into a parallel summary table
4. **Bipedal Southpaw Validation** -- rows match confirmation (non-split sanity check)
5. **Side-by-Side Comparison Table** -- Sofle vs Ambi LEFT vs Ambi RIGHT with columns: property, Sofle value, Ambi value, match?, action needed
6. **Matrix Row Doubling Convention** -- summarize the pattern: split boards double MATRIX_ROWS in vial.json (Sofle: 5 pins -> rows=10). Cite the QMK matrix.c ROWS_PER_HAND formula as evidence.

Use tables, not narrative paragraphs. Include source file paths for every value.
  </action>
  <verify>
File `.planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md` exists and contains:
- Sofle data table with matrix.rows=10 confirmed
- Ambi data table with matrix.rows=4 flagged as wrong
- Side-by-side comparison table with match/action columns
- Matrix row doubling convention explanation
  </verify>
  <done>First 6 sections of REFERENCE-ANALYSIS.md are complete with all comparison data.</done>
</task>

<task type="auto">
  <name>Task 2: Add EEPROM calculations, position maps, and encoder comparison</name>
  <files>
    .planning/phases/01-reference-analysis/EXTRACTED-DATA.md
    .planning/phases/01-reference-analysis/REFERENCE-ANALYSIS.md
  </files>
  <action>
Append to REFERENCE-ANALYSIS.md (created in Task 1) the remaining analysis sections:

7. **EEPROM Layer Size Impact** -- show the formula `layer_size = rows * cols * 2` and calculate:
   - Current Ambi: 4 * 8 * 2 = 64 bytes/layer (WRONG -- Vial uses this, firmware expects 128)
   - Correct Ambi: 8 * 8 * 2 = 128 bytes/layer
   - This explains the layer offset: Vial writes layer 1 at byte 64 but firmware reads layer 1 at byte 128

8. **Encoder Configuration Comparison** -- Sofle approach (encoder_map in keymap.c, no "encoders" JSON key) vs Ambi approach (has "encoders" JSON key in right vial.json). Document what must stay the same and what the approach difference means.

9. **Right-Half Layout Position Map** -- list EVERY position string from the right vial.json (extracted in EXTRACTED-DATA.md). For each position, show: current row, target row (current + 4), col (unchanged). This is the line-by-line change map for Phase 3.

10. **Thumbstick Position Verification** -- confirm left-half thumbstick positions at rows 0-3, col 7 remain valid after the fix (they are in the left half's row range and do not need adjustment).

The document should be self-contained: a reader should understand the full matrix convention and what needs to change without reading any other file.
  </action>
  <verify>
REFERENCE-ANALYSIS.md now contains all 10 sections:
- EEPROM layer size calculation (64 vs 128 bytes)
- Encoder configuration comparison
- Right-half layout position map with current and target row numbers for every position
- Thumbstick position verification
  </verify>
  <done>REFERENCE-ANALYSIS.md is complete with all verified data, comparison tables, position maps, and EEPROM calculations ready for Phase 2+ to consume.</done>
</task>

</tasks>

<verification>
Phase 1 Success Criteria check (reference document):
1. Sofle rev1 Vial config.h MATRIX_ROWS value documented alongside vial.json rows value, confirming they match -- verify REFERENCE-ANALYSIS.md contains "rows: 10" for both sources
2. Sofle rev1 vial.json layout positions for right half documented with row numbers -- verify the document lists rows 5-9 for right-half keys
3. Sofle rev1 encoder mapping documented -- verify encoder configuration comparison section exists
4. "What Sofle does that Ambi must also do" checklist -- verified in Plan 03
</verification>

<success_criteria>
- REFERENCE-ANALYSIS.md exists with 10 sections covering all comparison data
- Every value cited has a source file path (carried from EXTRACTED-DATA.md)
- Side-by-side comparison table identifies all mismatches
- Right-half layout position map lists all key positions with current and target row numbers
- EEPROM calculation shows 64 vs 128 byte discrepancy
</success_criteria>

<output>
After completion, create `.planning/phases/01-reference-analysis/01-02-SUMMARY.md`
</output>
