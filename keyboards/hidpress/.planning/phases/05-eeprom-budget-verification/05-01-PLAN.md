---
phase: 05-eeprom-budget-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-eeprom-budget-verification/EEPROM-ADDRESS-MAP.md
autonomous: true

must_haves:
  truths:
    - "Every EEPROM region (eeconfig, VIA header, dynamic keymap, encoder map, macros) has a documented byte range with start address, end address, and size"
    - "No two EEPROM regions overlap -- every adjacent pair has end_A < start_B"
    - "eeconfig_user (bytes 19-22) is confirmed non-overlapping with VIA/dynamic keymap regions (starting at byte 36+)"
    - "Total EEPROM usage equals exactly 4096 bytes (WEAR_LEVELING_LOGICAL_SIZE) with macros filling the remainder"
    - "All addresses are verified against vial-qmk fork source code (dynamic_keymap.c, via.h, eeconfig.h), not upstream QMK docs"
    - "Left-half and right-half maps are documented separately showing the encoder map difference"
  artifacts:
    - path: ".planning/phases/05-eeprom-budget-verification/EEPROM-ADDRESS-MAP.md"
      provides: "Complete EEPROM address map for both halves with overlap verification"
      contains: "Byte Range"
  key_links:
    - from: "quantum/eeconfig.h"
      to: "EEPROM-ADDRESS-MAP.md"
      via: "EECONFIG_SIZE = sizeof(eeprom_core_t) = 36"
      pattern: "EECONFIG_SIZE.*36"
    - from: "quantum/dynamic_keymap.c"
      to: "EEPROM-ADDRESS-MAP.md"
      via: "Address chain: VIA_EEPROM_CONFIG_END -> keymap -> encoders -> macros"
      pattern: "DYNAMIC_KEYMAP_EEPROM_ADDR"
    - from: "quantum/via.h"
      to: "EEPROM-ADDRESS-MAP.md"
      via: "VIA_EEPROM_MAGIC_ADDR = EECONFIG_SIZE = 36"
      pattern: "VIA_EEPROM_MAGIC_ADDR"
---

<objective>
Verify the EEPROM address chain from vial-qmk source code and produce a documented EEPROM address map for both halves of the Bipedal Ambi, confirming no region overlaps and total usage fits within 4096 bytes.

Purpose: Satisfy EEPR-04 (EEPROM budget documented with no overlaps) and XVAL-02 (verified against vial-qmk fork source, not upstream docs). This is the final gate before defensive EEPROM improvements in Phase 6.

Output: `.planning/phases/05-eeprom-budget-verification/EEPROM-ADDRESS-MAP.md`
</objective>

<execution_context>
@/Users/matthewthomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewthomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-eeprom-budget-verification/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify EEPROM address constants against vial-qmk source and produce documented address map</name>
  <files>.planning/phases/05-eeprom-budget-verification/EEPROM-ADDRESS-MAP.md</files>
  <action>
Read the following source files from the vial-qmk fork to extract the exact EEPROM address chain constants. Do NOT use the research values blindly -- read each source file and confirm each constant matches.

**Step 1: Read source files and extract constants**

Read these files and extract the specific values listed:

1. `quantum/eeconfig.h` -- Find the `eeprom_core_t` struct definition. Count the PACKED struct size field-by-field:
   - magic (uint16_t = 2), debug (uint8_t = 1), default_layer (uint8_t = 1), keymap (uint16_t = 2), backlight (uint8_t = 1), audio (uint8_t = 1), rgblight (uint32_t = 4), unicode (uint8_t = 1), steno (uint8_t = 1), handedness (uint8_t = 1), keyboard (uint32_t = 4), user (uint32_t = 4), union of led_matrix(uint32_t)/rgb_matrix(uint64_t) = max(4,8) = 8, haptic (uint32_t = 4), rgblight_ext (uint8_t = 1)
   - Confirm total = 36 bytes
   - Extract EECONFIG_SIZE formula: EECONFIG_BASE_SIZE + EECONFIG_KB_DATA_SIZE + EECONFIG_USER_DATA_SIZE

2. `quantum/via.h` -- Extract:
   - VIA_EEPROM_MAGIC_ADDR = EECONFIG_SIZE
   - VIA_EEPROM_LAYOUT_OPTIONS_ADDR = VIA_EEPROM_MAGIC_ADDR + 3
   - VIA_EEPROM_LAYOUT_OPTIONS_SIZE (default 1)
   - VIA_EEPROM_CUSTOM_CONFIG_ADDR = LAYOUT_OPTIONS_ADDR + LAYOUT_OPTIONS_SIZE
   - VIA_EEPROM_CUSTOM_CONFIG_SIZE (default 0)
   - VIA_EEPROM_CONFIG_END = CUSTOM_CONFIG_ADDR + CUSTOM_CONFIG_SIZE

3. `quantum/dynamic_keymap.c` -- Extract the complete address chain:
   - DYNAMIC_KEYMAP_EEPROM_ADDR = VIA_EEPROM_CONFIG_END
   - Keymap size formula = DYNAMIC_KEYMAP_LAYER_COUNT * MATRIX_ROWS * MATRIX_COLS * 2
   - VIAL_ENCODERS_EEPROM_ADDR = DYNAMIC_KEYMAP_EEPROM_ADDR + keymap_size
   - VIAL_ENCODERS_SIZE formula (NUM_ENCODERS * DYNAMIC_KEYMAP_LAYER_COUNT * 2 * 2)
   - VIAL_QMK_SETTINGS_EEPROM_ADDR through VIAL_KEY_OVERRIDE_EEPROM_ADDR (all zero-sized for this keyboard)
   - DYNAMIC_KEYMAP_MACRO_EEPROM_ADDR
   - DYNAMIC_KEYMAP_EEPROM_MAX_ADDR = TOTAL_EEPROM_BYTE_COUNT - 1
   - The _Static_assert requiring >= 100 bytes for macros

4. `platforms/chibios/drivers/wear_leveling/wear_leveling_rp2040_flash_config.h` -- Extract:
   - WEAR_LEVELING_BACKING_SIZE (expect 8192)
   - WEAR_LEVELING_LOGICAL_SIZE formula (BACKING_SIZE / 2 = 4096)

5. `keyboards/hidpress/bipedalambi/config.h` -- Confirm MATRIX_ROWS=8, MATRIX_COLS=8
6. `keyboards/hidpress/bipedalambi/keymaps/vial_right/config.h` -- Confirm DYNAMIC_KEYMAP_LAYER_COUNT=4, NUM_ENCODERS=1
7. `keyboards/hidpress/bipedalambi/keymaps/vial_left/config.h` -- Confirm DYNAMIC_KEYMAP_LAYER_COUNT=4, no encoder defines
8. `keyboards/hidpress/bipedalambi/keymaps/vial_right/rules.mk` -- Confirm ENCODER_ENABLE=yes, ENCODER_MAP_ENABLE=yes
9. `keyboards/hidpress/bipedalambi/keymaps/vial_left/rules.mk` -- Confirm ENCODER_MAP_ENABLE is NOT enabled

Also verify that EECONFIG_KB_DATA_SIZE and EECONFIG_USER_DATA_SIZE are NOT defined by the keyboard (search all config.h files in keyboards/hidpress/bipedalambi/).

**Step 2: Calculate addresses for both halves**

Using the verified constants, compute the complete address chain for both the right half (with encoder) and left half (without encoder). Show all arithmetic.

Expected results (verify, do not assume):
- Right half: eeconfig 0-35, VIA magic 36-38, VIA layout 39, dynamic keymap 40-551, encoder map 552-567, macros 568-4095
- Left half: eeconfig 0-35, VIA magic 36-38, VIA layout 39, dynamic keymap 40-551, macros 552-4095

**Step 3: Verify no overlaps**

For each pair of adjacent regions, confirm end_of_region_N < start_of_region_N+1 (or they are exactly adjacent: end + 1 = start). For eeconfig_user specifically, confirm bytes 19-22 end well before VIA magic starts at byte 36.

**Step 4: Write EEPROM-ADDRESS-MAP.md**

Create `.planning/phases/05-eeprom-budget-verification/EEPROM-ADDRESS-MAP.md` with:

1. **Header** -- Title, date, purpose, requirements satisfied (EEPR-04, XVAL-02)
2. **Constants Table** -- Every constant used, its value, and the source file + line reference where it was read
3. **Right Half Address Map** -- Table with columns: Byte Range, Size (bytes), Region Name, Source File. Include sub-entries for eeconfig_user at bytes 19-22 and per-layer keymap ranges.
4. **Left Half Address Map** -- Same format, showing the difference (no encoder map region)
5. **Overlap Verification** -- For each adjacent pair of regions, show: Region A end, Region B start, gap/adjacency. Explicitly confirm eeconfig_user (19-22) does not overlap VIA magic (36-38). State the result: "No overlaps detected."
6. **Budget Summary** -- Total fixed usage for each half, remaining macro space, percentage of 4096. Confirm _Static_assert passes.
7. **Source Cross-References** -- For each address in the map, cite the specific source file and the #define or struct that produces it. This satisfies XVAL-02 (verified against vial-qmk fork source).

Do NOT include the Python overlap script in the document -- perform the verification manually and state the results. The document is the deliverable, not a script.
  </action>
  <verify>
1. EEPROM-ADDRESS-MAP.md exists in `.planning/phases/05-eeprom-budget-verification/`
2. The document contains address maps for BOTH halves (left and right)
3. Every region's byte range is explicitly stated (start-end, size)
4. eeconfig_user bytes 19-22 are documented inside the eeconfig region
5. Overlap verification section confirms no overlaps
6. Budget summary shows total < 4096 bytes for both halves
7. Every address cites a vial-qmk source file (not upstream QMK/VIA docs)
8. The document covers all regions in the chain: eeconfig, VIA magic, VIA layout options, VIA custom config (0 bytes), dynamic keymap, encoder map (right only), QMK settings (0), tap dance (0), combos (0), key overrides (0), dynamic macros
  </verify>
  <done>
EEPROM-ADDRESS-MAP.md contains complete per-half address maps verified against vial-qmk source code. No region overlaps exist. eeconfig_user (bytes 19-22) is confirmed 13 bytes away from VIA magic (byte 36). Total usage fits within 4096 bytes for both halves. EEPR-04 and XVAL-02 are satisfied.
  </done>
</task>

</tasks>

<verification>
Phase 5 success criteria check:
1. ✓ Documented EEPROM address map shows byte ranges for all regions -- EEPROM-ADDRESS-MAP.md contains tables for both halves
2. ✓ Total EEPROM usage confirmed < 4096 bytes -- budget summary section with percentage
3. ✓ eeconfig_user confirmed non-overlapping with VIA/dynamic keymap -- overlap verification section with explicit byte range comparison
4. ✓ All calculations verified against vial-qmk fork source -- source cross-references section cites quantum/dynamic_keymap.c, quantum/via.h, quantum/eeconfig.h from this repo
</verification>

<success_criteria>
- EEPROM-ADDRESS-MAP.md exists with complete address maps for both halves
- No EEPROM region overlaps exist (verified in document)
- eeconfig_user (bytes 19-22) confirmed safe from VIA/keymap regions (byte 36+)
- Total EEPROM usage for each half fits within 4096-byte wear-leveling logical size
- Every address traces back to a vial-qmk fork source file reference (XVAL-02)
- Requirements EEPR-04 and XVAL-02 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-eeprom-budget-verification/05-01-SUMMARY.md`
</output>
