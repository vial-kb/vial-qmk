---
phase: 07-build-and-flash
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bipedalambi/config.h
  - bipedalambi/mcuconf.h
autonomous: true

must_haves:
  truths:
    - "Left half firmware compiles without errors"
    - "Right half firmware compiles without errors"
    - "Both UF2 files exist and are non-zero size"
    - "All working tree changes are committed before the build"
  artifacts:
    - path: ".build/hidpress_bipedalambi_vial_left.uf2"
      provides: "Left half firmware binary"
    - path: ".build/hidpress_bipedalambi_vial_right.uf2"
      provides: "Right half firmware binary"
  key_links:
    - from: "bipedalambi/config.h"
      to: ".build/hidpress_bipedalambi_vial_left.uf2"
      via: "make hidpress/bipedalambi:vial_left"
      pattern: "Compiling.*hidpress"
    - from: "bipedalambi/config.h"
      to: ".build/hidpress_bipedalambi_vial_right.uf2"
      via: "make hidpress/bipedalambi:vial_right"
      pattern: "Compiling.*hidpress"
---

<objective>
Commit pre-existing uncommitted configuration changes, then compile both left and right half firmwares from a clean build, resolving any build errors that arise.

Purpose: Produce UF2 firmware binaries containing all fixes from Phases 3-6 (matrix dimension fix, NUM_ENCODERS fix, eeconfig_init_user, EE_CLR) that the user can flash to both keyboard halves.
Output: Two UF2 firmware files in .build/ directory, ready for flashing.
</objective>

<execution_context>
@/Users/matthewthomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewthomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-build-and-flash/07-RESEARCH.md
@.planning/phases/06-defensive-eeprom-improvements/06-01-SUMMARY.md
@.planning/phases/04-peripheral-matrix-mapping/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit uncommitted config changes and clean build both halves</name>
  <files>bipedalambi/config.h, bipedalambi/mcuconf.h</files>
  <action>
There are two uncommitted changes in the working tree that affect compilation:

1. `bipedalambi/config.h`: Added `#define SERIAL_PIO_USE_PIO1` -- switches PIO serial driver from PIO0 to PIO1 (needed because WS2812 may claim PIO0 on the right half where WS2812_DI_PIN is still defined)
2. `bipedalambi/mcuconf.h`: Added `RP_SIO_USE_UART0 TRUE` and `RP_SIO_USE_UART1 TRUE` -- enables both UART peripherals in ChibiOS SIO driver (may be defensive/unnecessary but should not hurt)

These are pre-existing hardware configuration additions from before Phases 1-6, not changes made during the planning phases.

**Steps:**

1. Commit the two uncommitted files with message: `fix(07-01): commit pre-existing serial and UART config for build reproducibility`
   - Stage only `keyboards/hidpress/bipedalambi/config.h` and `keyboards/hidpress/bipedalambi/mcuconf.h`
   - These changes are already in the working tree and have been there since before Phase 1

2. Run `make clean` from the vial-qmk repo root (`/Users/matthewthomas/dev/vial-qmk/`) to remove stale .build/ artifacts from February 2

3. Build left half: `make hidpress/bipedalambi:vial_left` from the repo root
   - Expected output: `hidpress_bipedalambi_vial_left.uf2` in `.build/`
   - If build fails, examine the error output and fix the source issue

4. Build right half: `make hidpress/bipedalambi:vial_right` from the repo root
   - Expected output: `hidpress_bipedalambi_vial_right.uf2` in `.build/`
   - If build fails, examine the error output and fix the source issue

5. Verify both UF2 files exist and have non-zero size:
   - `ls -la .build/hidpress_bipedalambi_vial_*.uf2`

**If build errors occur:**
- For warnings-as-errors from GCC 15.2.1: try `make hidpress/bipedalambi:vial_left VERBOSE=true` to see the specific warning, then fix the source code (do NOT suppress with `-Wno-error` unless truly unavoidable)
- For missing header errors: verify git submodules with `git submodule status`
- For linker errors: check that all keymap.c function signatures match QMK expectations (eeconfig_init_user takes void, returns void)
- If a fix requires source code changes, make the minimal change, then rebuild

**Note:** Each `make` command may take 1-2 minutes. The builds are independent and sequential (left first, then right).
  </action>
  <verify>
Run all of these commands from `/Users/matthewthomas/dev/vial-qmk/`:

1. `git log --oneline -1` shows the config commit
2. `ls -la .build/hidpress_bipedalambi_vial_left.uf2` shows file exists with size > 0
3. `ls -la .build/hidpress_bipedalambi_vial_right.uf2` shows file exists with size > 0
4. Both `make` commands completed with exit code 0 (check `echo $?` after each)
  </verify>
  <done>
Both `make hidpress/bipedalambi:vial_left` and `make hidpress/bipedalambi:vial_right` exit with code 0, producing non-zero-size UF2 files in `.build/`. The uncommitted config.h and mcuconf.h changes are committed to git.
  </done>
</task>

</tasks>

<verification>
1. `git diff HEAD -- keyboards/hidpress/` shows no uncommitted changes (all config committed)
2. `.build/hidpress_bipedalambi_vial_left.uf2` exists and is > 50KB
3. `.build/hidpress_bipedalambi_vial_right.uf2` exists and is > 50KB
4. Both make commands completed without errors
</verification>

<success_criteria>
- Left half firmware compiles without errors
- Right half firmware compiles without errors
- Both UF2 files exist in .build/ with reasonable sizes (left ~125KB, right ~108KB based on previous builds)
- All keyboard source files are committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/07-build-and-flash/07-01-SUMMARY.md`
</output>
