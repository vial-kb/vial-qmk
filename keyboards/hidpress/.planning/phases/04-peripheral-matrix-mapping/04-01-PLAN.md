---
phase: 04-peripheral-matrix-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - keymaps/vial_right/config.h
autonomous: true

must_haves:
  truths:
    - "Left vial.json includes thumbstick positions at rows 0-3, col 7 matching matrix_scan_user dynamic_keymap_get_keycode calls"
    - "Right vial.json encoder KLE entries use encoder indices (0,0 and 0,1) not matrix row positions"
    - "Right vial.json 'encoders' JSON key is preserved with 1 encoder and 4 layers"
    - "NUM_ENCODERS is explicitly set to 1, preventing QMK split-doubling from creating phantom encoder"
    - "Both peripheral mappings are cross-referenced against Sofle rev1 conventions"
  artifacts:
    - path: "keymaps/vial_right/config.h"
      provides: "Explicit NUM_ENCODERS 1 define"
      contains: "#define NUM_ENCODERS 1"
  key_links:
    - from: "keymaps/vial_left/vial.json"
      to: "keymaps/vial_left/keymap.c"
      via: "thumbstick col 7 positions match dynamic_keymap_get_keycode rows"
      pattern: "dynamic_keymap_get_keycode.*[0-3], 7"
    - from: "keymaps/vial_right/config.h"
      to: "quantum/encoder.h"
      via: "NUM_ENCODERS define prevents split-doubling calculation"
      pattern: "#define NUM_ENCODERS 1"
---

<objective>
Verify thumbstick and encoder peripheral matrix positions in both vial.json files, fix the NUM_ENCODERS split-doubling mismatch, and cross-reference all peripheral mappings against Sofle rev1 conventions.

Purpose: Satisfies MTRX-05 (thumbstick positions) and MTRX-06 (encoder positions). The Phase 3 row remapping changed right-half KLE positions; this phase confirms the peripherals (which use special addressing -- virtual col 7 for thumbstick, encoder indices for encoder) survived correctly. Also fixes a NUM_ENCODERS=2 phantom encoder caused by QMK split-doubling logic.

Output: Updated keymaps/vial_right/config.h with NUM_ENCODERS fix, verification script output confirming all positions correct.
</objective>

<execution_context>
@/Users/matthewthomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewthomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-peripheral-matrix-mapping/04-RESEARCH.md

@keymaps/vial_left/vial.json
@keymaps/vial_right/vial.json
@keymaps/vial_left/keymap.c
@keymaps/vial_right/config.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify thumbstick and encoder positions with cross-reference validation</name>
  <files>keymaps/vial_left/vial.json, keymaps/vial_right/vial.json, keymaps/vial_left/keymap.c</files>
  <action>
Run the validation scripts from 04-RESEARCH.md to confirm all peripheral positions are correct. Specifically:

1. **Thumbstick verification (MTRX-05):** Parse left vial.json and confirm KLE entries exist at "0,7", "1,7", "2,7", "3,7". Then grep keymaps/vial_left/keymap.c for `dynamic_keymap_get_keycode` calls and confirm they reference rows 0, 1, 2, 3 at col 7. Both sides must match. All row values must be < 8 (within 8-row matrix bounds).

2. **Encoder KLE verification (MTRX-06 partial):** Parse right vial.json and confirm encoder KLE entries are "0,0\n...\ne" and "0,1\n...\ne" (using encoder indices, not matrix positions). Confirm no encoder KLE entries have row values >= 4 (which would indicate incorrect Phase 3 remapping).

3. **Encoder JSON key preservation:** Confirm right vial.json has top-level "encoders" key with structure: 1 encoder, 4 layers, each layer having [CCW_keycode, CW_keycode]. Confirm layer 0 is ["QK_KB_2", "QK_KB_1"] matching encoder_map in keymap.c.

4. **Sofle cross-reference:** Document comparison: Sofle uses ENCODER_MAP_ENABLE with 2 encoders and 4 KLE encoder entries. Ambi uses "encoders" JSON key with 1 encoder and 2 KLE encoder entries. Both use the same "\ne" suffix format. The difference in encoder configuration approach (Sofle's ENCODER_MAP_ENABLE vs Ambi's "encoders" JSON key) is a known documented difference from Phase 1, not a bug.

These are all read-only verifications. Do NOT modify any vial.json files -- they are already correct from Phase 3.
  </action>
  <verify>
Run python3 validation scripts from the research doc (Validations 1-5). All must print PASSED. Also run: `grep -n 'dynamic_keymap_get_keycode.*,.*7)' keymaps/vial_left/keymap.c` and confirm 4 lines matching rows 0-3.
  </verify>
  <done>
All 5 validation scripts pass. Thumbstick positions (0,7)-(3,7) confirmed in both vial.json and keymap.c. Encoder KLE entries confirmed as encoder indices (0,0 and 0,1). "encoders" JSON key preserved. Sofle cross-reference documented. MTRX-05 fully satisfied. MTRX-06 partially satisfied (KLE and JSON verified, NUM_ENCODERS fix in Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix NUM_ENCODERS split-doubling mismatch in right config.h</name>
  <files>keymaps/vial_right/config.h</files>
  <action>
Add `#define NUM_ENCODERS 1` to keymaps/vial_right/config.h in the encoder section (after the ENCODERS_PAD_B line, before the comment about pointing device). This prevents QMK's encoder.h split-doubling logic from setting NUM_ENCODERS=2 when only 1 physical encoder exists.

The fix works because encoder.h (line 69) has `#ifndef NUM_ENCODERS` -- explicitly defining it skips the automatic split-doubling calculation entirely.

**What this changes:**
- NUM_ENCODERS: 2 -> 1 (matches physical hardware)
- encoder_map array: 1 entry per layer (already correct, no keymap.c change needed)
- VIAL_ENCODERS_SIZE: 32 -> 16 bytes (saves 16 bytes EEPROM)
- EEPROM layout shifts after encoder region (already handled by Phase 7 mandatory EEPROM clear)

**What NOT to change:**
- Do NOT modify keymaps/vial_right/keymap.c -- encoder_map already has 1 entry per layer which is correct for NUM_ENCODERS=1
- Do NOT modify the "encoders" JSON key in vial.json -- it already has 1 encoder entry
- Do NOT add NUM_ENCODERS to the left side config.h -- the left side does not enable ENCODER_ENABLE in its rules.mk
  </action>
  <verify>
Read back keymaps/vial_right/config.h and confirm `#define NUM_ENCODERS 1` appears in the encoder section. Confirm ENCODERS_PAD_A and ENCODERS_PAD_B are still present and unchanged. Run: `grep -n 'NUM_ENCODERS\|ENCODERS_PAD' keymaps/vial_right/config.h` to verify all three defines exist.
  </verify>
  <done>
keymaps/vial_right/config.h contains `#define NUM_ENCODERS 1` in the encoder pin section. ENCODERS_PAD_A and ENCODERS_PAD_B are unchanged. MTRX-06 fully satisfied (encoder positions verified in Task 1, NUM_ENCODERS mismatch fixed here). Phase 4 complete.
  </done>
</task>

</tasks>

<verification>
1. All 5 python3 validation scripts from 04-RESEARCH.md pass (thumbstick positions, encoder KLE entries, encoders JSON key, cross-reference, bounds check)
2. `grep -c 'dynamic_keymap_get_keycode.*,.*7)' keymaps/vial_left/keymap.c` returns 8 (4 calls for press + 4 for release, using rows 0-3 col 7)
3. `grep 'NUM_ENCODERS' keymaps/vial_right/config.h` returns `#define NUM_ENCODERS 1`
4. keymaps/vial_left/vial.json is UNCHANGED from Phase 3 output (thumbstick positions already correct)
5. keymaps/vial_right/vial.json is UNCHANGED from Phase 3 output (encoder entries already correct)
</verification>

<success_criteria>
- MTRX-05: Left vial.json has thumbstick at rows 0-3 col 7, cross-referenced against matrix_scan_user
- MTRX-06: Right vial.json encoder KLE uses indices (0,0)/(0,1), "encoders" JSON key preserved, NUM_ENCODERS=1 in config.h
- Sofle cross-reference documented showing known differences (ENCODER_MAP_ENABLE vs "encoders" JSON key)
- Only file modified: keymaps/vial_right/config.h (one line added)
</success_criteria>

<output>
After completion, create `.planning/phases/04-peripheral-matrix-mapping/04-01-SUMMARY.md`
</output>
