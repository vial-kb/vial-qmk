#include "quantum.h"
#include "bipedalsouthpaw_shared.h"

extern enum pointing_device_mode current_mode;
extern int actuation;

#define ACTUATION_DISPLAY_DURATION 1000  // Show for 1 second


#ifdef OLED_ENABLE

// Layer state arrays
static const char PROGMEM default_layer_on_mouse[] = {
    0x80, 0x81, 0x82, 0x83, 0x84, 0x85, 0x86, 0x87, 0x88, 0x89, 0x8A, 0x8B, 0x8C, 0x8D, 0x8E, 0x8F, 0x90, 0x31, 0x32, 0x93, 0x94,
    0xA0, 0xA1, 0xA2, 0xA3, 0xA4, 0xA5, 0xA6, 0xA7, 0xA8, 0xA9, 0xAA, 0xAB, 0xAC, 0xAD, 0xAE, 0xAF, 0xB0, 0x51, 0x52, 0xB3, 0xB4,
    0xC0, 0xC1, 0xC2, 0xC3, 0xC4, 0xC5, 0xC6, 0xC7, 0xC8, 0xC9, 0xCA, 0xCB, 0xCC, 0xCD, 0xCE, 0xCF, 0xD0, 0x71, 0x72, 0xD3, 0xD4, 0x00
};

static const char PROGMEM default_layer_on_scroll[] = {
    0x20, 0x21, 0x22, 0x23, 0x84, 0x85, 0x86, 0x87, 0x88, 0x89, 0x8A, 0x8B, 0x8C, 0x8D, 0x8E, 0x8F, 0x90, 0x31, 0x32, 0x93, 0x94,
    0x40, 0x41, 0x42, 0x43, 0xA4, 0xA5, 0xA6, 0xA7, 0xA8, 0xA9, 0xAA, 0xAB, 0xAC, 0xAD, 0xAE, 0xAF, 0xB0, 0x51, 0x52, 0xB3, 0xB4,
    0x60, 0x61, 0x62, 0x63, 0xC4, 0xC5, 0xC6, 0xC7, 0xC8, 0xC9, 0xCA, 0xCB, 0xCC, 0xCD, 0xCE, 0xCF, 0xD0, 0x71, 0x72, 0xD3, 0xD4, 0x00
};

static const char PROGMEM default_layer_on_keycodes[] = {
    0x95, 0x96, 0x97, 0x98, 0x84, 0x85, 0x86, 0x87, 0x88, 0x89, 0x8A, 0x8B, 0x8C, 0x8D, 0x8E, 0x8F, 0x90, 0x31, 0x32, 0x93, 0x94,
    0xB5, 0xB6, 0xB7, 0xB8, 0xA4, 0xA5, 0xA6, 0xA7, 0xA8, 0xA9, 0xAA, 0xAB, 0xAC, 0xAD, 0xAE, 0xAF, 0xB0, 0x51, 0x52, 0xB3, 0xB4,
    0xD5, 0xD6, 0xD7, 0xD8, 0xC4, 0xC5, 0xC6, 0xC7, 0xC8, 0xC9, 0xCA, 0xCB, 0xCC, 0xCD, 0xCE, 0xCF, 0xD0, 0x71, 0x72, 0xD3, 0xD4, 0x00
};

static const char PROGMEM one_layer_on_mouse[] = {
    0x80, 0x81, 0x82, 0x83, 0x84, 0x85, 0x86, 0x87, 0x88, 0x89, 0x8A, 0x8B, 0x8C, 0x8D, 0x2E, 0x2F, 0x90, 0x91, 0x92, 0x93, 0x94,
    0xA0, 0xA1, 0xA2, 0xA3, 0xA4, 0xA5, 0xA6, 0xA7, 0xA8, 0xA9, 0xAA, 0xAB, 0xAC, 0xAD, 0x4E, 0x4F, 0xB0, 0xB1, 0xB2, 0xB3, 0xB4,
    0xC0, 0xC1, 0xC2, 0xC3, 0xC4, 0xC5, 0xC6, 0xC7, 0xC8, 0xC9, 0xCA, 0xCB, 0xCC, 0xCD, 0x6E, 0x6F, 0xD0, 0xD1, 0xD2, 0xD3, 0xD4, 0x00
};

static const char PROGMEM one_layer_on_scroll[] = {
    0x20, 0x21, 0x22, 0x23, 0x84, 0x85, 0x86, 0x87, 0x88, 0x89, 0x8A, 0x8B, 0x8C, 0x8D, 0x2E, 0x2F, 0x90, 0x91, 0x92, 0x93, 0x94,
    0x40, 0x41, 0x42, 0x43, 0xA4, 0xA5, 0xA6, 0xA7, 0xA8, 0xA9, 0xAA, 0xAB, 0xAC, 0xAD, 0x4E, 0x4F, 0xB0, 0xB1, 0xB2, 0xB3, 0xB4,
    0x60, 0x61, 0x62, 0x63, 0xC4, 0xC5, 0xC6, 0xC7, 0xC8, 0xC9, 0xCA, 0xCB, 0xCC, 0xCD, 0x6E, 0x6F, 0xD0, 0xD1, 0xD2, 0xD3, 0xD4, 0x00
};

static const char PROGMEM one_layer_on_keycodes[] = {
    0x95, 0x96, 0x97, 0x98, 0x84, 0x85, 0x86, 0x87, 0x88, 0x89, 0x8A, 0x8B, 0x8C, 0x8D, 0x2E, 0x2F, 0x90, 0x91, 0x92, 0x93, 0x94,
    0xB5, 0xB6, 0xB7, 0xB8, 0xA4, 0xA5, 0xA6, 0xA7, 0xA8, 0xA9, 0xAA, 0xAB, 0xAC, 0xAD, 0x4E, 0x4F, 0xB0, 0xB1, 0xB2, 0xB3, 0xB4,
    0xD5, 0xD6, 0xD7, 0xD8, 0xC4, 0xC5, 0xC6, 0xC7, 0xC8, 0xC9, 0xCA, 0xCB, 0xCC, 0xCD, 0x6E, 0x6F, 0xD0, 0xD1, 0xD2, 0xD3, 0xD4, 0x00
};

static const char PROGMEM two_layer_on_mouse[] = {
    0x80, 0x81, 0x82, 0x83, 0x84, 0x85, 0x86, 0x87, 0x88, 0x89, 0x8A, 0x2B, 0x2C, 0x8D, 0x8E, 0x8F, 0x90, 0x91, 0x92, 0x93, 0x94,
    0xA0, 0xA1, 0xA2, 0xA3, 0xA4, 0xA5, 0xA6, 0xA7, 0xA8, 0xA9, 0xAA, 0x4B, 0x4C, 0xAD, 0xAE, 0xAF, 0xB0, 0xB1, 0xB2, 0xB3, 0xB4,
    0xC0, 0xC1, 0xC2, 0xC3, 0xC4, 0xC5, 0xC6, 0xC7, 0xC8, 0xC9, 0xCA, 0x6B, 0x6C, 0xCD, 0xCE, 0xCF, 0xD0, 0xD1, 0xD2, 0xD3, 0xD4, 0x00
};

static const char PROGMEM two_layer_on_scroll[] = {
    0x20, 0x21, 0x22, 0x23, 0x84, 0x85, 0x86, 0x87, 0x88, 0x89, 0x8A, 0x2B, 0x2C, 0x8D, 0x8E, 0x8F, 0x90, 0x91, 0x92, 0x93, 0x94,
    0x40, 0x41, 0x42, 0x43, 0xA4, 0xA5, 0xA6, 0xA7, 0xA8, 0xA9, 0xAA, 0x4B, 0x4C, 0xAD, 0xAE, 0xAF, 0xB0, 0xB1, 0xB2, 0xB3, 0xB4,
    0x60, 0x61, 0x62, 0x63, 0xC4, 0xC5, 0xC6, 0xC7, 0xC8, 0xC9, 0xCA, 0x6B, 0x6C, 0xCD, 0xCE, 0xCF, 0xD0, 0xD1, 0xD2, 0xD3, 0xD4, 0x00
};

static const char PROGMEM two_layer_on_keycodes[] = {
    0x95, 0x96, 0x97, 0x98, 0x84, 0x85, 0x86, 0x87, 0x88, 0x89, 0x8A, 0x2B, 0x2C, 0x8D, 0x8E, 0x8F, 0x90, 0x91, 0x92, 0x93, 0x94,
    0xB5, 0xB6, 0xB7, 0xB8, 0xA4, 0xA5, 0xA6, 0xA7, 0xA8, 0xA9, 0xAA, 0x4B, 0x4C, 0xAD, 0xAE, 0xAF, 0xB0, 0xB1, 0xB2, 0xB3, 0xB4,
    0xD5, 0xD6, 0xD7, 0xD8, 0xC4, 0xC5, 0xC6, 0xC7, 0xC8, 0xC9, 0xCA, 0x6B, 0x6C, 0xCD, 0xCE, 0xCF, 0xD0, 0xD1, 0xD2, 0xD3, 0xD4, 0x00
};

static const char PROGMEM three_layer_on_mouse[] = {
    0x80, 0x81, 0x82, 0x83, 0x84, 0x85, 0x86, 0x87, 0x28, 0x29, 0x8A, 0x8B, 0x8C, 0x8D, 0x8E, 0x8F, 0x90, 0x91, 0x92, 0x93, 0x94,
    0xA0, 0xA1, 0xA2, 0xA3, 0xA4, 0xA5, 0xA6, 0xA7, 0x48, 0x49, 0xAA, 0xAB, 0xAC, 0xAD, 0xAE, 0xAF, 0xB0, 0xB1, 0xB2, 0xB3, 0xB4,
    0xC0, 0xC1, 0xC2, 0xC3, 0xC4, 0xC5, 0xC6, 0xC7, 0x68, 0x69, 0xCA, 0xCB, 0xCC, 0xCD, 0xCE, 0xCF, 0xD0, 0xD1, 0xD2, 0xD3, 0xD4, 0x00
};

static const char PROGMEM three_layer_on_scroll[] = {
    0x20, 0x21, 0x22, 0x23, 0x84, 0x85, 0x86, 0x87, 0x28, 0x29, 0x8A, 0x8B, 0x8C, 0x8D, 0x8E, 0x8F, 0x90, 0x91, 0x92, 0x93, 0x94,
    0x40, 0x41, 0x42, 0x43, 0xA4,0xA5, 0xA6, 0xA7, 0x48, 0x49, 0xAA, 0xAB, 0xAC, 0xAD, 0xAE, 0xAF, 0xB0, 0xB1, 0xB2, 0xB3, 0xB4,
    0x60, 0x61, 0x62, 0x63, 0xC4, 0xC5, 0xC6, 0xC7, 0x68, 0x69, 0xCA, 0xCB, 0xCC, 0xCD, 0xCE, 0xCF, 0xD0, 0xD1, 0xD2, 0xD3, 0xD4, 0x00
};

static const char PROGMEM three_layer_on_keycodes[] = {
    0x95, 0x96, 0x97, 0x98, 0x84, 0x85, 0x86, 0x87, 0x28, 0x29, 0x8A, 0x8B, 0x8C, 0x8D, 0x8E, 0x8F, 0x90, 0x91, 0x92, 0x93, 0x94,
    0xB5, 0xB6, 0xB7, 0xB8, 0xA4, 0xA5, 0xA6, 0xA7, 0x48, 0x49, 0xAA, 0xAB, 0xAC, 0xAD, 0xAE, 0xAF, 0xB0, 0xB1, 0xB2, 0xB3, 0xB4,
    0xD5, 0xD6, 0xD7, 0xD8, 0xC4, 0xC5, 0xC6, 0xC7, 0x68, 0x69, 0xCA, 0xCB, 0xCC, 0xCD, 0xCE, 0xCF, 0xD0, 0xD1, 0xD2, 0xD3, 0xD4, 0x00
};

// Actuation level arrays
static const char PROGMEM actuation_level_1[] = {
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x3E, 0x3F, 0x1F, 0x9D, 0x1F, 0x84, 0x85, 0x86, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x5E, 0x5F, 0x1F, 0xBD, 0x1F, 0xA4, 0xA5, 0xA6, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x7E, 0x7F, 0x1F, 0xDD, 0x1F, 0xC4, 0xC5, 0xC6, 0x1F, 0x1F, 0x1F, 0x1F, 0x00
};

static const char PROGMEM actuation_level_2[] = {
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x3C, 0x3D, 0x1F, 0x9D, 0x1F, 0x84, 0x85, 0x86, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x5C, 0x5D, 0x1F, 0xBD, 0x1F, 0xA4, 0xA5, 0xA6, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x7C, 0x7D, 0x1F, 0xDD, 0x1F, 0xC4, 0xC5, 0xC6, 0x1F, 0x1F, 0x1F, 0x1F, 0x00
};

static const char PROGMEM actuation_level_3[] = {
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x3A, 0x3B, 0x1F, 0x9D, 0x1F, 0x84, 0x85, 0x86, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x5A, 0x5B, 0x1F, 0xBD, 0x1F, 0xA4, 0xA5, 0xA6, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x7A, 0x7B, 0x1F, 0xDD, 0x1F, 0xC4, 0xC5, 0xC6, 0x1F, 0x1F, 0x1F, 0x1F, 0x00
};

static const char PROGMEM actuation_level_4[] = {
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x38, 0x39, 0x1F, 0x9D, 0x1F, 0x84, 0x85, 0x86, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x58, 0x59, 0x1F, 0xBD, 0x1F, 0xA4, 0xA5, 0xA6, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x78, 0x79, 0x1F, 0xDD, 0x1F, 0xC4, 0xC5, 0xC6, 0x1F, 0x1F, 0x1F, 0x1F, 0x00
};

static const char PROGMEM actuation_level_5[] = {
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x36, 0x37, 0x1F, 0x9D, 0x1F, 0x84, 0x85, 0x86, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x56, 0x57, 0x1F, 0xBD, 0x1F, 0xA4, 0xA5, 0xA6, 0x1F, 0x1F, 0x1F, 0x1F,
    0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x76, 0x77, 0x1F, 0xDD, 0x1F, 0xC4, 0xC5, 0xC6, 0x1F, 0x1F, 0x1F, 0x1F, 0x00
};

// static const char PROGMEM hidpress_logo[] = {
//     0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x24, 0x44, 0x64, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F,
//     0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x25, 0x45, 0x65, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F,
//     0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x26, 0x46, 0x66, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x1F, 0x00
// };

// Layer state rendering function
void render_layer_state(void) {
    uint8_t layer = get_highest_layer(layer_state);

    switch(layer) {
        case _DEFAULT:
            switch(current_mode) {
                case MODE_MOUSE:
                    oled_write_P(default_layer_on_mouse, false);
                    break;
                case MODE_SCROLLING:
                    oled_write_P(default_layer_on_scroll, false);
                    break;
                case MODE_CUSTOM_KEYS:
                    oled_write_P(default_layer_on_keycodes, false);
                    break;
                default:
                    oled_write_P(PSTR("MODE: UNKNOWN\n"), false);
                    break;
            }
            break;
        case _ONE:
            switch(current_mode) {
                case MODE_MOUSE:
                    oled_write_P(one_layer_on_mouse, false);
                    break;
                case MODE_SCROLLING:
                    oled_write_P(one_layer_on_scroll, false);
                    break;
                case MODE_CUSTOM_KEYS:
                    oled_write_P(one_layer_on_keycodes, false);
                    break;
                default:
                    oled_write_P(PSTR("LAYER 1: UNKNOWN MODE\n"), false);
                    break;
            }
            break;
        case _TWO:
            switch(current_mode) {
                case MODE_MOUSE:
                    oled_write_P(two_layer_on_mouse, false);
                    break;
                case MODE_SCROLLING:
                    oled_write_P(two_layer_on_scroll, false);
                    break;
                case MODE_CUSTOM_KEYS:
                    oled_write_P(two_layer_on_keycodes, false);
                    break;
                default:
                    oled_write_P(PSTR("LAYER 2: UNKNOWN MODE\n"), false);
                    break;
            }
            break;
        case _THREE:
            switch(current_mode) {
                case MODE_MOUSE:
                    oled_write_P(three_layer_on_mouse, false);
                    break;
                case MODE_SCROLLING:
                    oled_write_P(three_layer_on_scroll, false);
                    break;
                case MODE_CUSTOM_KEYS:
                    oled_write_P(three_layer_on_keycodes, false);
                    break;
                default:
                    oled_write_P(PSTR("LAYER 3: UNKNOWN MODE\n"), false);
                    break;
            }
            break;
        default:
            oled_write_P(PSTR("LAYER: UNKNOWN\n"), false);
            break;
    }
}

uint32_t actuation_display_timer = 0;
bool showing_actuation = false;

void render_actuation_state(void) {
    switch(actuation) {
        case 64:
            oled_write_P(actuation_level_5, false);
            break;
        case 128:
            oled_write_P(actuation_level_4, false);
            break;
        case 256:  // State 3 (default)
            oled_write_P(actuation_level_3, false);
            break;
        case 320:
            oled_write_P(actuation_level_2, false);
            break;
        case 352:
            oled_write_P(actuation_level_1, false);
            break;
        default:
            oled_write_P(actuation_level_3, false);
            break;
    }
}

// Startup splash screen
#define SPLASH_DURATION 2000  // Show splash for 2 seconds
static uint32_t splash_timer = 0;
static bool splash_shown = false;
static bool splash_initialized = false;

// Screensaver state
static uint32_t last_activity_time = 0;
static bool screensaver_active = false;

// Animation particle system
#define NUM_PARTICLES 50
#define SCREEN_CENTER_X 64  // Center of 128-pixel wide display
#define SCREEN_CENTER_Y 16  // Center of 32-pixel tall display
#define FRAME_DELAY 40      // ~25 FPS

// Warp particle structure - linear movement to center
typedef struct {
    int16_t x;       // Current position (scaled by 16)
    int16_t y;
    int16_t dx;      // Velocity X (scaled by 16)
    int16_t dy;      // Velocity Y
    int16_t prev_x;  // Previous position for trails
    int16_t prev_y;
    int16_t prev2_x; // Second previous position
    int16_t prev2_y;
} warp_particle_t;

static warp_particle_t warp_particles[NUM_PARTICLES];
static uint32_t animation_timer = 0;
static bool warp_initialized = false;

// Simple pseudo-random number generator
static uint16_t rng_state = 12345;
static uint16_t simple_rand(void) {
    rng_state ^= rng_state << 7;
    rng_state ^= rng_state >> 9;
    rng_state ^= rng_state << 8;
    return rng_state;
}





// Initialize a single warp particle - spawn on edges/corners of screen
static void init_warp_particle(warp_particle_t *p) {
    // Spawn on edges of the screen for a better warp effect
    // Screen is vertically oriented (32 wide x 128 tall in visual space)
    // Top/bottom are short edges, left/right are long edges
    // Distribution: top 40%, bottom 40%, left 10%, right 10%
    int16_t edge_rand = simple_rand() % 100;

    if (edge_rand < 40) {
        // Top edge (40%)
        p->x = (simple_rand() % 128) * 16;
        p->y = 0;
    } else if (edge_rand < 80) {
        // Bottom edge (40%)
        p->x = (simple_rand() % 128) * 16;
        p->y = 31 * 16;
    } else if (edge_rand < 90) {
        // Left edge (10%)
        p->x = 0;
        p->y = (simple_rand() % 32) * 16;
    } else {
        // Right edge (10%)
        p->x = 127 * 16;
        p->y = (simple_rand() % 32) * 16;
    }

    // Calculate velocity toward center
    int16_t target_x = SCREEN_CENTER_X * 16;
    int16_t target_y = SCREEN_CENTER_Y * 16;

    int16_t diff_x = target_x - p->x;
    int16_t diff_y = target_y - p->y;

    // Calculate distance (approximate using octagonal method)
    int16_t abs_dx = diff_x < 0 ? -diff_x : diff_x;
    int16_t abs_dy = diff_y < 0 ? -diff_y : diff_y;
    int16_t dist = abs_dx > abs_dy ? abs_dx + (abs_dy >> 1) : abs_dy + (abs_dx >> 1);

    // Normalize and set speed (2-4 pixels per frame, scaled)
    int16_t speed = 32 + (simple_rand() % 32);  // 2-4 * 16

    if (dist > 16) {  // Ensure minimum distance
        p->dx = (diff_x * speed) / dist;
        p->dy = (diff_y * speed) / dist;
    } else {
        // If too close to center, give a default velocity
        p->dx = 16;
        p->dy = 8;
    }

    // Initialize trail positions to current position
    p->prev_x = p->x;
    p->prev_y = p->y;
    p->prev2_x = p->x;
    p->prev2_y = p->y;
}

// Initialize all warp particles
static void init_warp(void) {
    rng_state = timer_read() | 1;  // Seed with timer (ensure odd)
    for (int i = 0; i < NUM_PARTICLES; i++) {
        init_warp_particle(&warp_particles[i]);
    }
}

// Update warp particle positions
static void update_warp(bool respawn) {
    int16_t target_x = SCREEN_CENTER_X * 16;
    int16_t target_y = SCREEN_CENTER_Y * 16;

    for (int i = 0; i < NUM_PARTICLES; i++) {
        warp_particle_t *p = &warp_particles[i];

        // Store previous positions for trails
        p->prev2_x = p->prev_x;
        p->prev2_y = p->prev_y;
        p->prev_x = p->x;
        p->prev_y = p->y;

        // Move toward center
        p->x += p->dx;
        p->y += p->dy;

        // Check if reached center (within threshold)
        int16_t diff_x = target_x - p->x;
        int16_t diff_y = target_y - p->y;
        int16_t abs_dx = diff_x < 0 ? -diff_x : diff_x;
        int16_t abs_dy = diff_y < 0 ? -diff_y : diff_y;

        if (abs_dx < 32 && abs_dy < 32) {  // Within 2 pixels of center
            if (respawn) {
                init_warp_particle(p);
            } else {
                // Mark as dead (at center)
                p->x = target_x;
                p->y = target_y;
                p->dx = 0;
                p->dy = 0;
            }
        }
    }
}

// Render warp particles to OLED with streaking trails
static void render_warp(uint8_t count) {
    oled_clear();

    int16_t target_x = SCREEN_CENTER_X * 16;
    int16_t target_y = SCREEN_CENTER_Y * 16;

    // Draw particles with trails
    for (int i = 0; i < count && i < NUM_PARTICLES; i++) {
        warp_particle_t *p = &warp_particles[i];

        // Skip dead particles (at center with no velocity)
        if (p->dx == 0 && p->dy == 0 && p->x == target_x && p->y == target_y) continue;

        int16_t px, py;

        // Draw main pixel (current position)
        px = p->x / 16;
        py = p->y / 16;
        if (px >= 0 && px < 128 && py >= 0 && py < 32) {
            oled_write_pixel(px, py, true);
        }

        // Draw trail point 1 (previous position)
        px = p->prev_x / 16;
        py = p->prev_y / 16;
        if (px >= 0 && px < 128 && py >= 0 && py < 32) {
            oled_write_pixel(px, py, true);
        }

        // Draw trail point 2 (second previous position)
        px = p->prev2_x / 16;
        py = p->prev2_y / 16;
        if (px >= 0 && px < 128 && py >= 0 && py < 32) {
            oled_write_pixel(px, py, true);
        }

        // Draw brighter particles near center for convergence glow
        int16_t dist_x = p->x - target_x;
        int16_t dist_y = p->y - target_y;
        int16_t abs_dx = dist_x < 0 ? -dist_x : dist_x;
        int16_t abs_dy = dist_y < 0 ? -dist_y : dist_y;

        if (abs_dx < 80 && abs_dy < 80) {  // Within ~5 pixels of center
            px = p->x / 16;
            py = p->y / 16;
            if (px > 0 && px < 127) {
                oled_write_pixel(px - 1, py, true);
                oled_write_pixel(px + 1, py, true);
            }
            if (py > 0 && py < 31) {
                oled_write_pixel(px, py - 1, true);
                oled_write_pixel(px, py + 1, true);
            }
        }
    }

    // Draw bright center core
    if (count > 10) {
        int radius_sq = (count > 40) ? 4 : (count > 20 ? 1 : 0);
        for (int dy = -2; dy <= 2; dy++) {
            for (int dx = -2; dx <= 2; dx++) {
                if (dx*dx + dy*dy <= radius_sq) {
                    oled_write_pixel(SCREEN_CENTER_X + dx, SCREEN_CENTER_Y + dy, true);
                }
            }
        }
    }
}

// Register activity to reset screensaver timer
void register_oled_activity(void) {
    last_activity_time = timer_read32();
    if (screensaver_active) {
        screensaver_active = false;
        warp_initialized = false;  // Reset so warp reinits next time
        oled_clear();
    }
}

bool oled_task_user(void) {
    static bool was_showing_actuation = false;

    // Initialize on first run
    if (!splash_initialized) {
        oled_clear();
        init_warp();  // Initialize warp for startup animation
        warp_initialized = true;
        splash_timer = timer_read32();
        last_activity_time = timer_read32();
        splash_initialized = true;
    }

    // Show animated warp on startup
    if (!splash_shown) {
        if (timer_elapsed32(animation_timer) > FRAME_DELAY) {
            uint32_t elapsed = timer_elapsed32(splash_timer);
            bool respawn = true;
            uint8_t render_count = NUM_PARTICLES;

            // Fade out in the last 1000ms of splash
            #define FADE_DURATION 1000
            if (elapsed > (SPLASH_DURATION - FADE_DURATION)) {
                respawn = false;  // Stop respawning

                // Reduce particle count over time
                uint32_t fade_elapsed = elapsed - (SPLASH_DURATION - FADE_DURATION);
                if (fade_elapsed > FADE_DURATION) fade_elapsed = FADE_DURATION;

                // Linear fade: reduce from NUM_PARTICLES to 0
                render_count = NUM_PARTICLES - (NUM_PARTICLES * fade_elapsed / FADE_DURATION);
            }

            update_warp(respawn);
            render_warp(render_count);
            animation_timer = timer_read32();
        }
        if (timer_elapsed32(splash_timer) > SPLASH_DURATION) {
            splash_shown = true;
            warp_initialized = false;  // Reset for screensaver use
            oled_clear();
        }
        return false;
    }

    // Check for screensaver activation
    if (!screensaver_active && timer_elapsed32(last_activity_time) > SCREENSAVER_TIMEOUT) {
        screensaver_active = true;
    }

    // Render screensaver if active - use warp animation
    if (screensaver_active) {
        if (timer_elapsed32(animation_timer) > FRAME_DELAY) {
            // Init warp if not yet initialized
            if (!warp_initialized) {
                init_warp();
                warp_initialized = true;
            }
            update_warp(true);  // Always respawn
            render_warp(NUM_PARTICLES); // Always render all
            animation_timer = timer_read32();
        }
        return false;
    }

    // Normal display logic
    if (showing_actuation) {
        // If we just started showing actuation
        if (!was_showing_actuation) {
            oled_clear();
            was_showing_actuation = true;
        }

        if (timer_elapsed32(actuation_display_timer) > ACTUATION_DISPLAY_DURATION) {
            showing_actuation = false;
            was_showing_actuation = false;
            oled_clear();
            render_layer_state();
        } else {
            render_actuation_state();
        }
    } else {
        if (was_showing_actuation) {
            oled_clear();
            was_showing_actuation = false;
        }
        render_layer_state();
    }

    return false;
}

#endif
